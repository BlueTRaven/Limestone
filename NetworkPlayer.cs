using System;
using System.Collections.Generic;

using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;

using Limestone.Utility;

namespace Limestone
{
    /*public class NetworkPlayer : EntityLiving
    {
        public override Vector2 center { get; set; }

        public Rectangle hitbox { get { return new Rectangle(position.ToPoint(), new Vector2(32, 32).ToPoint()); } set { } }
        
        public NetworkPlayer(Vector2 position)
        {
            this.position = position;
        }

        public override void Die(World world)
        {
        }

        public override void Draw(SpriteBatch batch)
        {
            DrawGeometry.DrawRectangle(batch, hitbox, Color.Blue);
        }

        public override void DrawOutline(SpriteBatch batch)
        {
        }

        public override void Update(World world)
        {
        }
    }*/
}
